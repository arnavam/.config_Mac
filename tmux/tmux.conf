# set-environment -g PATH "/usr/local/bin:/bin:/usr/bin"
unbind r 
bind r source-file ~/.config/tmux/tmux.conf

# Set prefix
set-option -g status-position bottom
# unbind C-b
# set -g prefix C-Space 
# bind C-Space send-prefix


# set -g status-bg black # dark background
# set -g status-fg colour248      # light foreground
#
# # Left side of status bar: session name and window list
# set -g status-left-length 40
# set -g status-left "#[fg=green]#S #[default]"
#
# # Window title style when inactive
# set-window-option -g window-status-style fg=colour244,bg=default
#
# # Window title style for the active window
# set-window-option -g window-status-current-style fg=colour166,bg=default,bold
#
# # Format for inactive windows: window index and name
# set-window-option -g window-status-format " #I: #W "
#
# # Format for active window: window index and name highlighted
# set-window-option -g window-status-current-format " #[fg=yellow]#I: #W#[default] "
#
# # Right side of status bar: system time and date
# set -g status-right "%Y-%m-%d %H:%M #[fg=cyan]#(date +'%a, %b %d')#[default]"
#
# set -g status-right "#(acpi | cut -d',' -f2) | %Y-%m-%d %H:%M"

# Enable mouse support (optional)
set -g mouse on
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

set -g mouse on
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

set -g @plugin 'catppuccin/tmux#v2.1.3' # See https://github.com/catppuccin/tmux/tags for additional tags
set -g @catppuccin_window_status_style "rounded"
set -g status-left ""
set -g status-right ""
set -g status-right-length 100
set -g status-left-length 100
 # set -g status-left "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_window}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

run '~/.tmux/plugins/tpm/tpm'
